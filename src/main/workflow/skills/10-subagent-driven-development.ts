import { SkillDef } from './types'

export const skill: SkillDef = {
  name: 'subagent-driven-development',
  description: '当任务太大、步骤超过 8 步、或可以拆成多个互相独立的子任务时使用。',
  tiers: null,
  isStage: false,
  ironLaw: '子任务间要有明确依赖顺序。每个子任务完成后必须可独立验证。不要同时派发多个子 agent — 按依赖顺序执行。',
  redFlags: [
    '子任务没有明确边界',
    '子任务间有潜在代码冲突还并行执行',
    '拆分得太细（每步不是一个有意义的变更）',
    '任务不超过 3 步也拆',
    '没做集成验证就标记完成',
  ],
  rationalizations: [
    {
      excuse: '这些子任务看起来没有依赖，可以并行',
      reality: '没有共享文件 ≠ 没有依赖。集成测试阶段会发现 hidden dependency。串行更安全。',
    },
    { excuse: '拆细一点子 agent 好做', reality: '每步至少是一个有意义的变更。太细的子任务导致 review 成本 > 开发收益。' },
  ],
  promptModule: `## 📦 子任务拆分技能（已激活）

### 铁律
子任务间要有明确依赖顺序。不要并行派发有潜在冲突的子 agent。

### 流程
1. 拆解任务 — 按功能边界拆成独立子任务
   - 每个子任务是一个有意义的变更
   - 标记依赖关系
2. 制定计划 — create_dev_plan 包含所有子任务步骤
3. 串行执行 — 按依赖顺序逐一实现
   - 每完成一个子任务，验证通过再进入下一个
4. 集成验证 — 全部完成后整体测试

### 什么情况需要拆分
- 步骤 > 8 步
- 涉及多个独立模块（不共享代码路径）
- 多个独立功能点
- 需要不同角色（前端 + 后端 + 数据库）

### 什么情况不需要拆分
- 步骤 < 5 步且耦合紧密
- 改动集中在一个文件
- 纯文本/配置修改

### 审查清单
- [ ] 子任务边界清晰？
- [ ] 依赖顺序正确？
- [ ] 每步可独立验证？
- [ ] 每步是一个有意义的变更？
- [ ] 集成验证通过？

### Hard Gates
- 串行执行，不要同时派发多个有潜在冲突的子 agent
- 每个子任务完成后必须可独立验证`,
  antiPatterns: ['不要同时改所有模块 — 按顺序来', '不要拆分得太细（每步至少是一个有意义的变更）', '不要并行派发可能冲突的子 agent'],
}
