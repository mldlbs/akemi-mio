import { SkillDef } from './types'

export const skill: SkillDef = {
  name: 'migration',
  description: '当需要数据迁移、配置迁移、代码从旧方案迁移到新方案时使用。要求有回滚计划。',
  tiers: null,
  isStage: false,
  ironLaw: '没有回滚方案的迁移不是迁移，是赌博。前向兼容 + 后向兼容必须同时考虑。',
  redFlags: ['没有回滚计划就动手', '迁移和功能改动混在一起', '没有验证新旧数据一致性', '一次性迁移所有（没有灰度）', '改完不通知相关方'],
  rationalizations: [
    { excuse: '这个迁移很简单，不会出问题', reality: '迁移从来不是"简单"的。如果简单，写个回滚方案也很简单。' },
    { excuse: '先迁了再说，有问题再修', reality: '数据迁移出问题 = 数据丢失。回滚方案必须在动手之前写好。' },
  ],
  promptModule: `## 📦 迁移技能（已激活）

### 铁律
没有回滚方案的迁移不是迁移，是赌博。

### 流程
1. **方案设计**
   - 迁移范围：哪些数据/配置/代码
   - 迁移策略：big bang 还是逐步迁移
   - 回滚方案：每个步骤的可逆方式
2. **前向兼容**
   - 旧代码能读新数据
   - 新代码能读旧数据
3. **执行**
   - 先在 staging 环境验证
   - 监控指标：成功率/延迟/错误率
4. **验证**
   - 数据一致性检查
   - 功能回归测试
5. **清理**
   - 确认稳定后清理旧代码/旧数据
   - 回滚方案转为历史记录

### 迁移策略对比
| 策略 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| Big Bang | 简单直接 | 风险高、回滚困难 | 小数据量、低风险 |
| 逐步迁移 | 风险可控 | 需要兼容层 | 大数据量、高要求 |
| 双写+切换 | 零停机 | 复杂度高 | 线上服务 |

### 审查清单
- [ ] 回滚方案已写好？
- [ ] 前向兼容验证过？
- [ ] 数据一致性有验证方式？
- [ ] staging 环境验证通过？
- [ ] 监控指标已确认？

### Hard Gates
- 必须写好回滚方案再动手
- 迁移和功能改动不能混在同一个提交`,
  antiPatterns: ['不要没有回滚方案就做迁移', '不要迁移和功能改动混在一起', '不要跳过 staging 环境验证', '不要不验证数据一致性'],
}
