appId: com.akemi-mio.wallpaper
productName: AkemiMio
directories:
  output: dist-electron
  buildResources: build
afterPack: build/afterPack.js
files:
  - "out/**/*"
  - "package.json"
  - "CONSTITUTION.md"
  - "!models/**"
  - "!out/**/*.map"
  - "!out/**/*.txt"
  - "!out/electron.vite.config.*"
  - "!out/tsconfig*.tsbuildinfo"
  - "!out/src/**"
  - "!docs/**"
  - "!.claude*/**"
  - "!.githooks/**"
  - "!.swarm/**"
  - "!.openclaude/**"
  - "!.prettierrc"
  - "!.aiguard.yml"
  - "!src/**"
  - "!mcp_workspace/**"
  - "!evolution_workspace/**"
  - "!vitest.config.*"
  - "!eslint.config.*"
  - "!tsconfig*.json"
  - "!**/tsconfig*.json"
  - "!logs.txt"
  - "!logs/**"
  - "!**/node_modules/**/*.map"
# MCP 服务器是 spawn 出来的**独立 node 进程**，裸 node 读不了 asar 里的文件
# （表现为 Cannot find module ...app.asar\node_modules\@playwright\mcp\cli.js）。
# 所以这几个包必须解包到 app.asar.unpacked，运行时再把路径指过去。
asarUnpack:
  - "node_modules/@playwright/mcp/**/*"
  - "node_modules/playwright/**/*"
  - "node_modules/playwright-core/**/*"
win:
  icon: build/icon.ico
  target:
    - target: nsis
      arch:
        - x64
  artifactName: "${productName}-Setup-${version}.${ext}"
  signAndEditExecutable: false
nsis:
  oneClick: false
  perMachine: true
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: AkemiMio
  uninstallDisplayName: "AkemiMio 秋山澪 AI 语音助手"
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
extraResources:
  - from: "."
    filter:
      - "icon.png"
  # Piper TTS 的解释器脚本必须以「文件」形式随包发布：主进程是用子进程
  # `python <script>` 调它的，裸 python 读不了 asar 里的成员。
  # 运行时由 core/config 的 resolvePiperScript() 同步到 <userData>/scripts/ 再执行，
  # 避免 userData 里残留的旧版本脚本（接口不兼容）把新版本顶掉。
  - from: "scripts/piper_speak.py"
    to: "scripts/piper_speak.py"
npmRebuild: false
electronDist: node_modules/electron/dist
publish:
  - provider: generic
    url: "https://skills.crlkcloud.cyou/update"
    channel: latest
