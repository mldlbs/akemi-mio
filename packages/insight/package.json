{
  "name": "@akemi-mio/insight",
  "version": "0.1.0",
  "description": "Self-observation insight engine — detects patterns in memory and generates actionable insights",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
