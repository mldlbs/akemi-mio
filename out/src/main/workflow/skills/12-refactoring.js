export const skill = {
    name: 'refactoring',
    description: '当需要安全重构代码、提取公共逻辑、重命名符号、拆分大文件时使用。必须有测试保护。',
    tiers: null,
    isStage: false,
    ironLaw: '重构前必须有测试覆盖。没有测试的重构不是重构，是重写。',
    redFlags: ['重构前没跑测试确认基线', '一次重构太多东西', '边重构边加功能', '重构后不跑回归测试', '手动重命名不更新引用'],
    rationalizations: [
        { excuse: '重构就是改代码结构，不用测试覆盖', reality: '没有测试的重构叫乱改。你不知道改完后原来功能是否正常。' },
        { excuse: '顺手重构一下，也不费事', reality: '重构和 feature 不能混在同一次提交里。分开。' },
    ],
    promptModule: `## 🔧 重构技能（已激活）

### 铁律
重构前必须有测试覆盖。没有测试的重构是重写，不是重构。

### 流程
1. **基线确认** — run_command 跑全部测试，确认基线通过
2. **小步重构** — 每次只做一个原子重构操作
   - 重命名 → 提取函数 → 提取类 → 移动文件
   - 每步后跑测试确认
3. **回滚方案** — 每步可回滚
4. **回归测试** — 全部重构完成后跑完整测试

### 安全的原子重构操作
| 操作 | 说明 | 风险 |
|------|------|------|
| Rename | 重命名符号，IDE工具批量更新引用 | 低 |
| Extract Function | 抽取重复代码为独立函数 | 低 |
| Extract Class | 把一组相关数据和方法抽成类 | 中 |
| Move File | 移动文件并更新 import | 中 |
| Inline | 内联不必要的间接层 | 低 |
| Split File | 拆分大文件为多个小文件 | 高，需额外小心 |

### 审查清单
- [ ] 重构前基线测试通过？
- [ ] 每步原子操作后测试通过？
- [ ] 重构不包含功能变更？
- [ ] 回归测试全部通过？
- [ ] 重构和 feature 分开提交？

### Hard Gates
- 重构前必须有测试覆盖
- 重构和功能改动不能混在同一个提交`,
    antiPatterns: ['不要在没有测试覆盖的代码上重构', '不要一次做多个原子重构操作', '不要边重构边加功能', '不要重构后不跑回归测试'],
};
