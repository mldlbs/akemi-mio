node_modules
dist
dist-electron
dist-electron*/
build/icon-preview.png
*.exe
.env
pnpm-lock.yaml
pnpm-workspace.yaml
*.zip
.DS_Store
Thumbs.db
models
logs
logs.*
*.log
*.bak
*.tmp
.idea       
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.scc
*.cache
*.whl
*.tar.gz
*.wav
*.ps1
_check*
_typecheck*
# site 部署探针留下的临时输出（HTTP 状态码 / ssh 报错转录），可随时重生成
_final.txt
_live_check.txt
_redeploy.txt
check-*.bat
check-*.cmd
check-*.js
check-*.mjs
scripts/query-shadow.*
__*
!__tests__/
# `__*` 本意是拦临时探针（`__typecheck_probe.ts` 之类），但它会连正经的测试辅助
# 模块一起吞掉：`tests/main/core/evaluation/__test_support__.ts` 被 3 个已跟踪的
# 测试文件 import，却一直没进库 —— 于是本机跑得通、CI 全新检出直接
# `Cannot find module`。别删这条否定，否则那两个测试在 CI 上会重新变红。
!**/__test_support__.ts
.claude/projects/
.claude/tool_call_log.json
.claude/tool-call-log-*.test.json
# electron-vite 每次构建落下的临时配置（electron.vite.config.<时间戳>.mjs）
electron.vite.config.*.mjs
# scripts/launch-*.cjs 的截图输出目录
.forms-capture/
.main-capture/
.launch-capture/
.launch.log
.claude-flow/
.swarm/
.playwright-mcp/
logs_json_only.txt
tools/
mcp_workspace/
src/main/credentials/
winCodeSign-*.7z
evolution_workspace/
.hermes/
*.db
coverage/
err*.txt
tsc*.txt
test-*.txt
test_write.txt
test*.json
writing-ui/
nul
scripts/cdp-*.mjs
scripts/cdp-*.cjs
scripts/fix-rct*.mjs
scripts/fix-rct*.cjs
scripts/mio-writing-session.mjs
scripts/mio-6g.cjs
scripts/mio-next.cjs
scripts/check-workflow.cjs
scripts/launch.cjs
scripts/merge-mcp.mjs
scripts/verify-*.cjs
reports/
TempRadar_*
C\357\200\272*
run-tsc.bat
scripts/m54-observe.mjs
scripts/probe*.py
scripts/patch_*.py
scripts/step3_*
scripts/m56-capability-migration-report.ts
# ⚠️ 凡**被测试 import** 的脚本必须 negate 回来，否则本机跑得通、CI 全新检出直接
# `Cannot find module` —— 测试文件是进库的，它 import 的模块却不是。
# 09-22 CI 实测：`tests/main/evolution/automation/__tests__/CapabilityMigrationReport.test.ts:3`
# 报 `Cannot find module '../../../../../scripts/m56-capability-migration-report'`。
# 上面 m56-observation-window 的 `!` 就是同一个坑的先例（`M56ObservationWindow.test.ts`
# import 它），只是当时漏了这一个。加新的 m56 脚本时先 grep 一下有没有测试引用。
!scripts/m56-capability-migration-report.ts
scripts/m56-identity-gap-report.ts
scripts/m56-observation-window.ts
!scripts/m56-observation-window.ts
scripts/m56-shadow-observe.ts
scripts/m56-backfill-identity.ts
scripts/m56-pilot-gate-check.ts
scripts/m56-pilot-health-check.ts
_run_tsc*
_run_tsc_check.*
_tsc_check.*
_check.mjs
check_tsc_tmp.txt
run_typecheck.cmd
out/
.ssh/
tsc-permission-test.json
*.ts.tmp.*
# `extensions/` 下只有 fanqie-mcp 一个扩展，而它是**产品的一部分**：
# `tests/main/tool/__tests__/FanqieProviderContract.test.ts:64` 会 spawn 它跑 MCP 协议、
# `ContentDraftingProviderContract.test.ts:49` 读它的源码做断言，
# `packages/main/src/bootstrap/AppRuntime.ts:578` 也用 `process.cwd()/extensions/...` 注册它。
# 它被忽略时这三个使用方在 CI 全新检出里全部失败（`Cannot find module` / `ENOENT`），
# 而本机因为文件就在那儿一直绿 —— 与 `__test_support__.ts`、`m56-*` 是同一类 bug。
#
# ⚠️ 不能写成 `extensions/` + `!extensions/fanqie-mcp/fanqie-mcp.mjs`：
# git 不允许 re-include 一个**父目录已被排除**的文件，必须把目录规则改成
# `extensions/*` 再逐层 negate。`extensions/fanqie-mcp/*` 保留「里面的其它文件仍
# 不入库」这个原意。
extensions/*
!extensions/fanqie-mcp/
extensions/fanqie-mcp/*
!extensions/fanqie-mcp/fanqie-mcp.mjs
test-user-data/
# 测试夹具：IdentityModule / MetaCycle 会把 constitution 临时写在仓库根目录，
# 跑完才删；清理失败时会残留，别让它被 `git add -A` 收进去
test-CONSTITUTION.md
test-meta-constitution.md
.worktrees/

# Temp art/design artifacts
tmp-*.png
temp-observation/

# Evolution system snapshots (stale branches)
evolution/snapshot/
C\357\200\272*

.mio-intelligence-smoke/
.mio-intelligence/

# Transient migration / scratch artifacts (not project source)
.migration-state/
.tmp/
# 损坏现场快照：诊断完就移到 D:\work\data\git-forensics\，别留在仓库里
.git-broken-*/

.workbuddy/
.workbuddy-ai/
site/

# Observer pipeline data. `mio observer collect` writes observations to
# <cwd>/.local/observer by design (project-local, unlike MIO_HOME), so running
# it inside a git repo would otherwise leave untracked data files behind.
.local/
